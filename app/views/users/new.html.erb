<% provide(:title, 'Create user') %>
<h1>Erstelle einen neuen Benutzer</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>

 			<%= render 'fields', f: f %>

      <%= f.submit "Erstelle Benutzer", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>